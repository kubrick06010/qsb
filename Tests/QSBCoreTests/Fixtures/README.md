Legacy reference fixtures live locally in `reference/winqsb` and are read from
there during compatibility tests.

Do not copy original WinQSB binaries, manuals, help files, bundled examples, or
decompressed derivatives into this committed fixture directory. Add files here
only when they are independently authored for this project or otherwise clearly
redistributable.
